# Cloudflare Pages SPA fallback.
# Any request that doesn't match a static file gets served the SPA shell
# (index.html) so react-router can handle client-side routing. Without
# this, deep-linking to /admin/login or /store/checkout returns 404.
/*    /index.html   200
