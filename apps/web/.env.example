# Copy this file to .env.local and adjust as needed.
# The base URL of the Spring Boot backend (apps/api).
# Leave blank for a pure-frontend demo (the contact form will simulate submit).
VITE_API_URL=http://localhost:9090
